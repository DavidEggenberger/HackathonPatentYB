<div class="card">
    <div class="card-body">
        
    </div>
    @foreach (var port in Node.Ports)
    {
        <PortRenderer Port="port" />
    }
</div>

@code {
    [Parameter]
    public EnergyRessourceNode Node { get; set; }
}
